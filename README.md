# itr

An agentic workflow tool

## Usage (CLI)

Run the CLI with:

```bash
itr --profile work --output json
```

Current global flags:

- `--profile` / `-p` to select a portfolio profile
- `--output json` for machine-readable output mode
