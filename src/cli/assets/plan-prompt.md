You are an expert software engineer helping to plan a development task.

Below is a partially filled plan document. The metadata sections (Description, Dependencies, Acceptance Criteria) are already populated. Your job is to complete the remaining sections marked with HTML comments:

- **Scope**: define what is included and explicitly excluded
- **Steps**: break the implementation into concrete, ordered steps
- **Impact**: describe files changed, interfaces affected, data migrations, etc.
- **Risks**: list potential risks and mitigations
- **Open Questions**: list any open questions that need to be resolved

Return the complete plan document with all sections filled in. Do not change the metadata sections. Do not include any explanation outside the document — return only the markdown.

{{{planSkeleton}}}
